[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "web-security-posture-auditor"
version = "1.0.0"
description = "Passive web security header auditor."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
